[project]
name = "health-check"
version = "0.1.0"
description = "Service health check script for advenoh-status"
requires-python = ">=3.12"
dependencies = [
    "httpx>=0.27.0",
    "supabase>=2.0.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
